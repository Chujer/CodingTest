#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

int main()
{
	long long a[3];
	cin >> a[0];
	cin >> a[1];
	cin >> a[2];

	cout << a[0] + a[1] + a[2];
	return 0;
}